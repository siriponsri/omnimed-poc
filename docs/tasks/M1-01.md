# M1-01 — Party, Patient and identifier schema

Status: READY_AFTER_DEPENDENCIES · Implementation: NOT_IMPLEMENTED

## Task contract

- Goal: Create minimal adult synthetic Party, Patient, self relationship and PatientIdentifier. HN generated atomically per tenant, unique and never reused. Tenant-scoped composite references and actor/time metadata. No HTTP patient routes in this task.
- Source: BR-CORE-01/02; BR-PAT-01-01/02/03/04; VR-PAT-01-02/03; C04/C07. Exact excerpts/locators: docs/source/SELECTED_SRS_EXCERPTS.md; trace: docs/REQUIREMENT_TRACE.md.
- Depends on: M0-01.
- Allowed areas: backend/app/patients/ (new); db/migrations/versions/0002_*.py; backend/tests/integration/; docs/DOMAIN_MODEL.md. Related trace/docs/tests may change; record additional areas before broadening.
- Acceptance: Live PostgreSQL up/down/up on empty test DB; concurrent HN allocation no duplicates; one preferred identifier per type; cross-tenant FK denied; no citizen ID fixture or clinical hard-delete path.
- Exclusions: other milestones, production PHI, real integrations, LLM in product, stock/CDS, cloud provisioning. D01 configuration does not authorize actual deployment.

## Execution and evidence

อ่าน AGENTS.md → POC_CONTRACT → .ai/TASK_TEMPLATE → task นี้ → excerpts → changed files. Builder ระบุขนาด diff และ migration/API contract ก่อนแก้; ไม่ refactor unrelated areas. ใช้หนึ่ง task/branch; รายงาน actual changed files ตอนจบ

รัน `python scripts/checks.py`; runtime/schema changes ต้อง `python scripts/checks.py --full` บน Docker host. Docs-only task ให้ระบุ unchanged gate evidence ที่ยังตรง candidate ไม่เขียน N/A เป็น PASS ลอย ๆ. Acceptance ด้านบนต้องมี meaningful tests; ห้ามใช้ skipped test แทน proof

ส่ง candidate digest + transcripts ให้ read-only SRS Guardian แล้ว Reviewer. ทั้งคู่ต้อง PASS ต่อ source เดียวกันก่อน complete. Normal repair <=2; hard blocker <=3; reroute ไม่ reset. SRS ambiguity/dependency missing ให้ BLOCKED_REQUIRES_HUMAN พร้อม Finding

## Handoff prompt

> Implement M1-01 only. Read AGENTS.md, docs/POC_CONTRACT.md, .ai/TASK_TEMPLATE.md, this task and relevant exact SRS excerpts. Confirm dependencies, keep synthetic-only data and deny-by-default protected routes. Update trace/docs, run gates, obtain read-only Guardian/Reviewer decisions. Report changed files, commands/results, candidate hash and findings.
