Command: standalone Docker Compose v2.39.2 config --quiet
Working directory: repository root, generated local .env kept outside package
Exit code: 0
Result: PASS (Compose syntax/configuration only)
The standalone Compose binary is test-environment tooling and is not distributed.
No Docker daemon, images, containers, live migration or volume persistence ran.

Isolated test override from scripts/compose_smoke.py also validated: !override and ephemeral loopback port syntax, EXIT 0. No containers started.
