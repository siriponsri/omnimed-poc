# Generate .env with: python scripts/dev.py init
# Synthetic local use only. Never commit .env.
POSTGRES_USER=omnimed
POSTGRES_PASSWORD=
POSTGRES_DB=omnimed_demo
APP_ENV=local
SYNTHETIC_DEMO_ONLY=true
NEXT_TELEMETRY_DISABLED=1
# Optional future developer tooling; no live provider in v0.1.
OPENROUTER_ENABLED=false
OPENROUTER_API_KEY=
